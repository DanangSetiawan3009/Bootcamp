import React, { Component } from 'react'

class Login extends Component {
    constructor(props) {
        super(props);
        this.state = { 
            // username : "admin",
            // password : "1234"
         };
    }

    // inputValue = e => {
    //     if ([e.target.name] === this.username ) {
            
    //     } else if ( [e.target.name] === this.password ) {

    //     } else {
    //         alert("Username / Password Salah")
    //     }
    // }

    render() {
        // if (this.props.menu === "login") {
        //     return (
                
        //     );
        // }
        return (
            <>
            </>
        );
    }
}

export default Login;